document.getElementById("msg_box").innerHTML= "<%= j render 'msg', conversation: @conversation, user: current_user %>";

var conversation = $("#msg_box");
var messages_list = conversation.find('.messages-list');
var height = messages_list[0].scrollHeight;
messages_list.scrollTop(height);